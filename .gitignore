# Next.js
.next/
out/

# Node
node_modules/

# Env files (keep .env.example, ignore real secrets)
.env
.env.local
.env.*.local

# Build artifacts
tsconfig.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Large source images for the spreadsheet workflow — these are compressed into
# public/signs/ on import, so the multi-MB originals must never enter git.
content/spreadsheet-workflow/images/

# Vercel
.vercel

# Claude Code local config
.claude/
.env*
